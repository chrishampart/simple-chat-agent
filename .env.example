# Google Cloud Configuration
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_CLOUD_LOCATION=europe-west2

# Optional: Staging bucket for agent deployment
STAGING_BUCKET=your-staging-bucket-name

# Optional: Specific Reasoning Engine ID (if not discovered dynamically)
# REASONING_ENGINE_ID=projects/your-project/locations/your-location/reasoningEngines/your-engine-id
